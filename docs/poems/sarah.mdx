---
title: "Sarah"
---

I'm using claude code remote. I look at deploy preview

I never use the local preview on docs `mint dev`

I just use the github pull request

Locking down sections of docs. ("PM is going to go power crazy")

- Was `@` Writer before

Only uses the dashboard for analytics Use the api for conversations

## How code changes happen.

> I spent the last month trying to get the automation in place.

They write docs in the codebase, we mirror the changes in the claude code docs repo that we have access to

they have a mono repo and they have a docs/ folder

they check which docs are available

---

- We only know "did not answer"
- We should have a "did answer correctly" / "did not hedge"

---

I am scraping all github issues and "code feedback"

> At 'writer' we didn't have enough customers to get customers feedback.

I wanted to triage things in terms of what matches my style guide.

Do you have a style guide?

"Agent readiness isn't top of mind"

Do you have a style guide?

Yeah it is its just a basic normal style guide etc

Defer to "Google Style Guide"

---

## User Journeys

Run through it as a persona as a customer. Try to "map out the user journey" I use this all the times for 'rewrites'

It spins up a Claude Code session, git clones a branch of the docs. Uses the `mint` cli and then clicks around on pages etc etc

---

She has been surfacing product issues for things like "this error doesn't make much sense" so now I just ship it myself