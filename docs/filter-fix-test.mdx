---
title: "🪄 Filter Fix Test"
description: "Exercises the newPages branch after flipping the docs.json filter"
---

Added after flipping the getPagesToSyncFromPaths filter from `endsWith('docs.json')` to `!endsWith('docs.json')`. Should show up in newPages.
